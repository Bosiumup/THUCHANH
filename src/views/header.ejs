<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        <% if (session && session.role === "admin") { %>
        <a class="navbar-brand" href="/dashboard">Dashboard</a>
        <% } else { %>
        <a class="navbar-brand" href="/">Trang Chủ</a>
        <% } %>
        <button
            class="navbar-toggler"
            type="button"
            data-bs-toggle="collapse"
            data-bs-target="#navbarNav"
            aria-controls="navbarNav"
            aria-expanded="false"
            aria-label="Toggle navigation"
        >
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <% if (session && session.role === "admin") { %>
                <li class="nav-item d-flex align-items-center">
                    <p class="mb-0 me-2">
                        Xin chào, <%= session.username %>! (Vai trò: <%=
                        session.role %>)
                    </p>
                    <a class="btn btn-danger" href="/logout">Đăng xuất</a>
                </li>
                <% } else if (session && session.role === "user") { %>
                <li class="nav-item">
                    <a class="nav-link" href="/about">Giới thiệu</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/contact">Liên hệ</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/detail-user"
                        >Trang thông tin chi tiết tài khoản</a
                    >
                </li>
                <li class="nav-item d-flex align-items-center">
                    <p
                        class="mb-0 me-2 btn btn-primary"
                        style="cursor: default"
                    >
                        Xin chào, <%= session.username %>! (Vai trò: <%=
                        session.role %>)
                    </p>
                    <a class="btn btn-danger" href="/logout">Đăng xuất</a>
                </li>
                <% } else { %>
                <li class="nav-item">
                    <a class="nav-link" href="/about">Giới thiệu</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/contact">Liên hệ</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/list-user"
                        >Danh sách người dùng</a
                    >
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/login">Đăng nhập</a>
                </li>
                <% } %>
            </ul>
        </div>
    </div>
</nav>
